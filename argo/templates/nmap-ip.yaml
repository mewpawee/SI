apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: nmap-ip
spec:
  serviceAccountName: argo
  artifactRepositoryRef:
    configMap: artifact-repositories
    key: default
  entrypoint: nmap-ip
  imagePullSecrets:
  - name: registry-sec
  arguments:
    parameters:
    - name: ip
      value: ''
    - name: version
      value: 'v1'
  templates:
  - name: nmap-ip
    inputs:
      parameters:
      - name: ip
      - name: version
    container:
      image: "registry.digitalocean.com/sec/nmap:{{inputs.parameters.version}}"
      command: [sh, -c]
      args:
      - nmap -v -A -p- -oX /tmp/nmap2hydra.xml -oN /tmp/log "{{inputs.parameters.ip}}";
    outputs:
      artifacts:
      - name: nmap
        path: "/tmp/"
        archive: 
          none: {}
      